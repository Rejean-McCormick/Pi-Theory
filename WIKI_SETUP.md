# GitHub Wiki Setup

GitHub stores a Wiki in a separate repository named:

```text
YOUR_REPOSITORY.wiki.git
```

Copy the contents of the `wiki/` directory into that Wiki repository.

The required landing page is `Home.md`.

All page filenames in this pack are ASCII-safe. The Wiki avoids fragile LaTeX delimiters and uses plain-text equations so the content renders consistently.
